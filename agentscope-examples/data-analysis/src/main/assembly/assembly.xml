<assembly xmlns="http://maven.apache.org/ASSEMBLY/2.2.0"
          xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
          xsi:schemaLocation="http://maven.apache.org/ASSEMBLY/2.2.0
          http://maven.apache.org/xsd/assembly-2.2.0.xsd">

    <id>dist</id>
    <formats>
        <format>tar.gz</format>
    </formats>
    <includeBaseDirectory>true</includeBaseDirectory>

    <!-- 1. 把 thin jar（仅含自己代码）放到 lib/ 目录 -->
    <files>
        <file>
            <source>${project.build.directory}/${project.artifactId}-${project.version}.jar</source>
            <outputDirectory>lib</outputDirectory>
            <destName>${project.artifactId}.jar</destName>
        </file>
    </files>

    <fileSets>
        <!-- 2. 把所有运行时依赖放到 lib/deps/ 目录 -->
        <fileSet>
            <directory>${project.build.directory}/deps</directory>
            <outputDirectory>lib/deps</outputDirectory>
        </fileSet>

        <!-- 3. 把 bin/ 目录下的启动脚本打进去 -->
        <fileSet>
            <directory>${project.basedir}/src/main/bin</directory>
            <outputDirectory>bin</outputDirectory>
            <fileMode>0755</fileMode>
        </fileSet>

        <!-- 4. 把线上配置文件放到 conf/ 目录（线上可直接修改此目录下的文件） -->
        <fileSet>
            <directory>${project.basedir}/src/main/resources</directory>
            <outputDirectory>conf</outputDirectory>
            <includes>
                <include>application.yml</include>
                <include>application-prod.yml</include>
                <include>logback.xml</include>
            </includes>
        </fileSet>

        <!-- 5. 把 prompts/ 提示词目录放到 conf/prompts/ -->
        <fileSet>
            <directory>${project.basedir}/src/main/resources/prompts</directory>
            <outputDirectory>conf/prompts</outputDirectory>
        </fileSet>
    </fileSets>

</assembly>
